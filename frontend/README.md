# Career Compass Frontend

Open this folder in Visual Studio Code and run:

```powershell
npm run dev
```

The frontend connects to `http://localhost:8081/api` by default. The project includes its installed packages for direct local use.

Available checks:

```powershell
npm run lint
npm run build
```
