# Career Compass Backend

Spring Boot REST API connected to the existing MySQL `career_compass` database.

Open this folder as an Existing Maven Project in Eclipse, then run `GraduatesOpportunitesApplication.java` as a Java Application or Spring Boot App.

The application does not automatically insert demo data. Existing MySQL records remain unchanged.
